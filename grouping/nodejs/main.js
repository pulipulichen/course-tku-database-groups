
const GROUP_NAME_PW_PAIR = require('./generate_group_name_pw_pair.js')

let main = async () => {

  for (let i = 0; i < GROUP_NAME_PW_PAIR.length; i++) {
    let {name, password} = GROUP_NAME_PW_PAIR[i]

    
  }
}

main()